<div class="container">
	<!-- Main View -->
	<div class="row">

		<!-- Main Blog View -->
		<div id="blog_view" class="span8">
			<% if @posts.any? %>
			<%= render 'posts' %>
					<%= will_paginate @posts %>
			<% end %>
		</div><!-- / Main Blog View -->

		<%= render 'sidebar' %>

	</div><!-- / Main View -->
</div>
