<%= form_for @post do |f| %>

	<% if @post.errors.any? %>
	  <div id="errorExplanation">
	    <h2><%= pluralize(@post.errors.count, "error") %> prohibited
	        this post from being saved:</h2>
	    <ul>
	    <% @post.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	  </div>
	<% end %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :content %>
	<%= text_area_tag :content, "", :class => "tinymce", :rows => 40, :cols => 120, :name => 'post[content]' %>

	<br>
	<%= tinymce %>

	<%= f.submit 'Post', class: 'btn btn-danger' %>
<% end %>